# Unity Drone Assembly & Flight Simulator

Игра на Unity, где нужно собрать квадрокоптер из запчастей, и после правильной сборки запускается симулятор полета.

## 🎮 Описание

Это интерактивная игра-симулятор, состоящая из двух основных частей:

1. **Сборка квадрокоптера** - перетаскивайте детали на правильные места на раме
2. **Симулятор полета** - управляйте собранным квадрокоптером в 3D пространстве

## ✨ Особенности

- 🧩 Интерактивная сборка квадрокоптера с drag & drop
- ✅ Проверка правильности установки деталей
- 🚁 Реалистичная физика полета квадрокоптера
- 🎯 Система управления с поддержкой всех осей (Pitch, Roll, Yaw)
- 📊 UI с отображением высоты, скорости и газа
- 🎨 Визуальная обратная связь при установке деталей

## Структура проекта

### Скрипты сборки (DroneAssembly)
- **DronePart.cs** - Компонент детали квадрокоптера, которую можно перетаскивать
- **PartSlot.cs** - Слот для установки детали с проверкой правильности
- **DroneAssemblyManager.cs** - Менеджер процесса сборки, проверяет завершенность

### Скрипты симулятора полета (FlightSimulator)
- **DroneController.cs** - Физика и управление квадрокоптером
- **CameraFollow.cs** - Камера, следующая за дроном
- **FlightUI.cs** - UI для отображения информации о полете

### Другие скрипты
- **SceneManager.cs** - Переключение между сценами
- **AssemblyUI.cs** - UI контроллер для сцены сборки

## 🚀 Быстрый старт

### Для конечного пользователя (без Unity)

**Цель**: Собрать игру один раз, чтобы она работала как обычное приложение.

1. **Установите Unity** (один раз):
   - Скачайте Unity Hub: https://unity.com/download
   - Установите Unity Editor 2020.3 LTS или новее

2. **Запустите скрипт сборки**:
   ```bash
   chmod +x build_and_run.sh
   ./build_and_run.sh
   ```

3. **Готово!** Игра будет в `Build/DroneGame.app`
   - Запуск: `open Build/DroneGame.app`
   - **Unity больше не нужен!**

📖 Подробнее: [LAUNCH_WITHOUT_UNITY.md](LAUNCH_WITHOUT_UNITY.md)

### Для разработчиков

1. **Создание деталей:**
   - Откройте `Tools > Create Drone Part`
   - Выберите тип детали и нажмите "Создать деталь"
   - Или используйте "Создать все детали" для автоматического создания всех необходимых частей

2. **Настройка сцены сборки:**
   - Откройте `Tools > Drone Assembly Setup Helper`
   - Нажмите "Создать базовую структуру сборки"
   - Нажмите "Найти все слоты и добавить в менеджер"

Подробные инструкции по настройке см. в файле `SETUP_INSTRUCTIONS.md`

## 📋 Настройка в Unity

### Сцена сборки (DroneAssembly)

1. **Создайте сцену "DroneAssembly"**

2. **Создайте раму квадрокоптера:**
   - Создайте пустой GameObject "DroneFrame"
   - Добавьте модель рамы или используйте простой куб
   - Добавьте компонент `PartSlot` с типом `Frame`

3. **Создайте слоты для деталей:**
   - Для каждого типа детали создайте пустой GameObject с компонентом `PartSlot`
   - Установите правильный `RequiredPartType` для каждого слота
   - Разместите слоты в нужных позициях на раме

4. **Создайте детали:**
   - Для каждой детали создайте GameObject с компонентом `DronePart`
   - Установите правильный `PartType`
   - Разместите детали в начальных позициях (можно использовать UI панель)

5. **Настройте менеджер:**
   - Создайте пустой GameObject "AssemblyManager"
   - Добавьте компонент `DroneAssemblyManager`
   - Назначьте все слоты в список `All Slots`

6. **Настройте UI:**
   - Создайте Canvas
   - Добавьте кнопку "Запустить полет"
   - Добавьте текст для отображения прогресса
   - Добавьте компонент `AssemblyUI` на Canvas

### Сцена симулятора полета (FlightSimulator)

1. **Создайте сцену "FlightSimulator"**

2. **Создайте квадрокоптер:**
   - Создайте GameObject "Drone"
   - Добавьте Rigidbody (масса ~1, использовать гравитацию)
   - Добавьте компонент `DroneController`
   - Создайте 4 пропеллера как дочерние объекты
   - Назначьте пропеллеры в `DroneController`

3. **Настройте камеру:**
   - Добавьте компонент `CameraFollow` на Main Camera
   - Установите квадрокоптер как цель

4. **Настройте UI:**
   - Создайте Canvas
   - Добавьте текстовые элементы для газа, высоты, скорости
   - Добавьте компонент `FlightUI` и назначьте ссылки

5. **Создайте окружение:**
   - Добавьте Plane для земли
   - Добавьте освещение
   - Добавьте препятствия или цели (опционально)

## 🎮 Управление в симуляторе

- **Space** - Увеличить газ
- **Left Shift** - Уменьшить газ
- **W/S** - Наклон вперед/назад (Pitch)
- **A/D** - Наклон влево/вправо (Roll)
- **Q/E** - Поворот влево/вправо (Yaw)
- **R** - Сбросить дрон в начальную позицию
- **Escape** - Вернуться к сборке
- **Tab** - Показать/скрыть панель управления

## 🧩 Необходимые детали для сборки

1. Рама (Frame) - основа квадрокоптера
2. 4 мотора (Motor1-4) - двигатели для пропеллеров
3. 4 пропеллера (Propeller1-4) - создают подъемную силу
4. Батарея (Battery) - источник питания
5. Контроллер полета (FlightController) - управляет полетом
6. Камера (Camera) - опционально, для видеосъемки

## 📁 Структура проекта

```
Assets/
├── Scripts/
│   ├── DroneAssembly/        # Скрипты системы сборки
│   │   ├── DronePart.cs
│   │   ├── PartSlot.cs
│   │   └── DroneAssemblyManager.cs
│   ├── FlightSimulator/      # Скрипты симулятора полета
│   │   ├── DroneController.cs
│   │   ├── CameraFollow.cs
│   │   ├── FlightUI.cs
│   │   └── FlightSimulatorManager.cs
│   ├── UI/                   # UI контроллеры
│   │   └── AssemblyUI.cs
│   ├── Utils/                # Вспомогательные утилиты
│   │   └── InputHelper.cs
│   ├── Editor/               # Редакторские инструменты
│   │   ├── AssemblySetupHelper.cs
│   │   └── PartCreator.cs
│   └── SceneManager.cs       # Менеджер сцен
```

## ⚙️ Требования

- Unity 2020.3 или новее
- .NET Framework 4.x
- Опционально: 3D модели для более реалистичного вида (можно использовать примитивы Unity)

## 💡 Примечания

- Все детали должны быть установлены правильно для запуска симулятора
- Симулятор использует реалистичную физику квадрокоптера с учетом распределения сил двигателей
- Можно настроить параметры двигателей, стабильности и управления в инспекторе
- Детали можно перетаскивать мышью (drag & drop)
- При правильной установке детали автоматически "прилипают" к слотам

## 🔧 Настройка параметров

### DroneController
- `Motor Power` - базовая мощность двигателей (рекомендуется: 1000)
- `Max Motor Power` - максимальная мощность (рекомендуется: 2000)
- `Stability` - коэффициент стабилизации (рекомендуется: 0.5)
- `Drag` - сопротивление воздуха (рекомендуется: 5)

### CameraFollow
- `Offset` - смещение камеры относительно дрона
- `Smooth Speed` - плавность следования (рекомендуется: 0.125)
- `Look At Target` - камера всегда смотрит на дрон

## 📦 Сборка игры

### Автоматическая сборка

```bash
./build_and_run.sh
```

Скрипт автоматически найдет Unity, создаст сцены, соберет игру и запустит её.

### Ручная сборка

1. Откройте проект в Unity
2. `File > Build Settings`
3. Добавьте сцены: `DroneAssembly` и `FlightSimulator`
4. Нажмите `Build`

📖 Подробнее: [README_BUILD.md](README_BUILD.md)

## 📝 Лицензия

Этот проект создан для образовательных целей. Вы можете свободно использовать и модифицировать код.

