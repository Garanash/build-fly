# 🚀 Быстрый запуск игры

## Автоматическая настройка (Рекомендуется)

Игра настроена для автоматической инициализации при запуске!

### Шаг 1: Откройте проект в Unity

1. Запустите Unity Hub
2. Откройте проект из папки `unity_fly`

### Шаг 2: Создайте сцены (если их нет)

#### Сцена сборки (DroneAssembly)

1. В Unity: `File > New Scene`
2. Сохраните как `DroneAssembly` в папке `Assets/Scenes/`
3. **Важно:** Добавьте пустой GameObject с именем "GameBootstrap"
4. Добавьте компонент `GameBootstrap` на этот объект
5. Нажмите Play ▶️

Игра автоматически создаст:
- ✅ Раму квадрокоптера
- ✅ Все слоты для деталей
- ✅ Все детали для сборки
- ✅ UI с кнопкой запуска и прогрессом

#### Сцена симулятора полета (FlightSimulator)

1. В Unity: `File > New Scene`
2. Сохраните как `FlightSimulator` в папке `Assets/Scenes/`
3. **Важно:** Добавьте пустой GameObject с именем "GameBootstrap"
4. Добавьте компонент `GameBootstrap` на этот объект
5. Нажмите Play ▶️

Игра автоматически создаст:
- ✅ Квадрокоптер с физикой
- ✅ Окружение (земля, освещение)
- ✅ Камеру, следующую за дроном
- ✅ UI с информацией о полете

### Шаг 3: Настройте Build Settings

1. `File > Build Settings`
2. Добавьте обе сцены:
   - `DroneAssembly` (первая, стартовая)
   - `FlightSimulator`
3. Убедитесь, что `DroneAssembly` отмечена как стартовая

### Шаг 4: Запустите игру!

1. Нажмите Play ▶️ в Unity Editor
2. Или соберите билд: `File > Build And Run`

## 🎮 Как играть

### Сцена сборки:
1. Перетащите детали мышью на правильные места на раме
2. Когда все детали установлены, кнопка "Запустить полет" станет активной
3. Нажмите кнопку для перехода в симулятор

### Симулятор полета:
- **Space** - Увеличить газ
- **Shift** - Уменьшить газ  
- **W/S** - Наклон вперед/назад
- **A/D** - Наклон влево/вправо
- **Q/E** - Поворот влево/вправо
- **R** - Сбросить дрон
- **Esc** - Вернуться к сборке
- **Tab** - Показать/скрыть панель управления

## 🔧 Ручная настройка (если нужно)

Если автоматическая настройка не работает:

1. **Для сцены сборки:**
   - Создайте пустой GameObject
   - Добавьте компонент `AutoAssemblySetup`
   - Нажмите правой кнопкой на компоненте → "Setup Assembly Scene"

2. **Для сцены полета:**
   - Создайте пустой GameObject
   - Добавьте компонент `AutoFlightSetup`
   - Нажмите правой кнопкой на компоненте → "Setup Flight Scene"

## ⚠️ Устранение проблем

### Детали не перетаскиваются
- Убедитесь, что у деталей есть коллайдеры
- Проверьте, что камера имеет тег "MainCamera"

### Дрон не взлетает
- Увеличьте `Motor Power` в `DroneController` (до 1500-2000)
- Проверьте, что Rigidbody использует гравитацию

### Кнопка "Запустить полет" неактивна
- Убедитесь, что все 11 деталей установлены
- Проверьте консоль Unity на наличие ошибок

### Сцена не загружается
- Убедитесь, что сцены добавлены в Build Settings
- Проверьте правильность имен сцен

## 📝 Примечания

- Все объекты создаются автоматически при первом запуске
- Можно использовать редакторские инструменты: `Tools > Create Drone Part` и `Tools > Drone Assembly Setup Helper`
- Для лучшей производительности используйте простые примитивы Unity (они уже создаются автоматически)

## 🎯 Готово!

Игра должна работать сразу после открытия в Unity и нажатия Play. Все настройки выполняются автоматически!

