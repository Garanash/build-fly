# 📦 Установка и запуск игры для конечного пользователя

## Вариант 1: Запуск через Unity Editor (Рекомендуется для разработки)

### Шаг 1: Установка Unity

1. Скачайте **Unity Hub**: https://unity.com/download
2. Установите **Unity Editor 2020.3 LTS** или новее через Unity Hub
3. Дождитесь завершения установки

### Шаг 2: Открытие проекта

1. Запустите **Unity Hub**
2. Нажмите **"Open"** или **"Add"**
3. Выберите папку проекта: `build-fly`
4. Unity откроет проект (первый раз может занять 2-5 минут)

### Шаг 3: Создание и запуск сцены

1. В Unity: `File > New Scene`
2. Сохраните: `File > Save As...`
   - Создайте папку `Assets/Scenes/` если её нет
   - Сохраните как `DroneAssembly.unity`
3. Создайте пустой объект:
   - `GameObject > Create Empty`
   - Назовите: `GameBootstrap`
4. Добавьте компонент:
   - Выберите `GameBootstrap` в Hierarchy
   - В Inspector нажмите `Add Component`
   - Найдите и добавьте: `GameBootstrap`
5. Нажмите **Play ▶️**

Игра автоматически создаст все необходимое!

## Вариант 2: Сборка и запуск готовой игры

### Шаг 1: Сборка через Unity Editor

1. Откройте проект в Unity (см. Вариант 1, Шаги 1-2)
2. Создайте сцены (см. Вариант 1, Шаг 3)
3. `File > Build Settings`
4. Добавьте сцены:
   - Перетащите `DroneAssembly.unity` в список
   - Перетащите `FlightSimulator.unity` в список (создайте аналогично)
5. Выберите платформу (macOS, Windows, Linux)
6. Нажмите `Build`
7. Выберите папку для сборки (например, `Build`)

### Шаг 2: Запуск собранной игры

#### macOS:
```bash
open Build/DroneGame.app
```

Или двойной клик по `DroneGame.app` в Finder

#### Windows:
```bash
Build\DroneGame.exe
```

#### Linux:
```bash
./Build/DroneGame.x86_64
```

## Вариант 3: Автоматическая сборка через скрипт

### macOS/Linux:

```bash
# Сделайте скрипт исполняемым
chmod +x build_game.sh

# Запустите сборку
./build_game.sh

# После сборки запустите игру
./run_game.sh
```

**Примечание:** Для работы скриптов нужен установленный Unity.

## 🎮 Что происходит при запуске

### Автоматически создается:

**Сцена сборки:**
- ✅ Рама квадрокоптера
- ✅ 11 слотов для деталей
- ✅ 11 деталей (рама, 4 мотора, 4 пропеллера, батарея, контроллер)
- ✅ UI с кнопкой и прогрессом
- ✅ Менеджер сборки

**Сцена симулятора:**
- ✅ Квадрокоптер с физикой
- ✅ Окружение (земля, освещение)
- ✅ Камера, следующая за дроном
- ✅ UI с информацией о полете
- ✅ Менеджер симулятора

## ⚠️ Требования системы

- **ОС:** macOS 10.14+, Windows 10+, Ubuntu 18.04+
- **Unity:** 2020.3 LTS или новее (для разработки)
- **Память:** Минимум 4 GB RAM
- **Место на диске:** ~500 MB для проекта + Unity Editor

## 🐛 Решение проблем

### "Unity не найден"
- Установите Unity Hub и Unity Editor
- Убедитесь, что Unity добавлен в PATH (для скриптов)

### "GameBootstrap не найден"
- Создайте пустой GameObject и добавьте компонент `GameBootstrap`

### "Сцена не загружается"
- Убедитесь, что сцены добавлены в Build Settings
- Проверьте правильность имен сцен

### "Детали не перетаскиваются"
- Убедитесь, что у деталей есть коллайдеры
- Проверьте, что Main Camera имеет тег "MainCamera"

## 📞 Дополнительная помощь

- См. файл `LAUNCH_GAME.md` для подробных инструкций
- См. файл `QUICK_START.md` для быстрого старта
- Проверьте консоль Unity (Window > General > Console) для ошибок

---

**Готово! Игра должна запуститься! 🚁**

