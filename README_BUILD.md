# 🔨 Сборка игры для конечного пользователя

## Цель

Собрать игру так, чтобы она работала **без Unity Editor**, как обычное приложение, которое можно просто скачать и запустить.

## 🚀 Автоматическая сборка (Рекомендуется)

### Шаг 1: Установите Unity (один раз)

1. Скачайте **Unity Hub**: https://unity.com/download
2. Установите **Unity Editor 2020.3 LTS** или новее
3. Дождитесь завершения установки

### Шаг 2: Запустите скрипт сборки

```bash
chmod +x build_and_run.sh
./build_and_run.sh
```

Скрипт автоматически:
- ✅ Найдет Unity на вашем компьютере
- ✅ Создаст необходимые сцены (если их нет)
- ✅ Соберет игру в готовое приложение
- ✅ Запустит игру

### Шаг 3: Готово!

После сборки игра будет находиться в папке `Build/DroneGame.app`

**Теперь игра работает БЕЗ Unity!** Просто запустите:
```bash
open Build/DroneGame.app
```

Или двойной клик по `DroneGame.app` в Finder.

## 📦 Ручная сборка через Unity Editor

Если автоматический скрипт не работает:

### 1. Откройте проект в Unity

1. Запустите Unity Hub
2. Нажмите "Open"
3. Выберите папку проекта

### 2. Создайте сцены

#### Сцена сборки:
1. `File > New Scene`
2. Сохраните как `DroneAssembly.unity` в `Assets/Scenes/`
3. Создайте пустой GameObject "GameBootstrap"
4. Добавьте компонент `GameBootstrap`

#### Сцена симулятора:
1. `File > New Scene`
2. Сохраните как `FlightSimulator.unity` в `Assets/Scenes/`
3. Создайте пустой GameObject "GameBootstrap"
4. Добавьте компонент `GameBootstrap`

### 3. Соберите игру

1. `File > Build Settings`
2. Добавьте обе сцены в список
3. Выберите платформу (macOS, Windows, Linux)
4. Нажмите `Build`
5. Выберите папку `Build` для сохранения

### 4. Запустите собранную игру

#### macOS:
```bash
open Build/DroneGame.app
```

#### Windows:
```bash
Build\DroneGame.exe
```

#### Linux:
```bash
./Build/DroneGame.x86_64
```

## 📤 Распространение игры

После сборки вы можете:

1. **Скопировать папку `Build/DroneGame.app`** на другой компьютер
2. **Заархивировать** и распространять через:
   - GitHub Releases
   - Облачное хранилище (Google Drive, Dropbox)
   - Свой сайт

3. **Создать установщик** (опционально):
   - macOS: используйте `create-dmg` для создания .dmg файла
   - Windows: используйте Inno Setup или NSIS
   - Linux: создайте .deb или .rpm пакет

## ⚠️ Важно

- **Собранная игра НЕ требует Unity** - это standalone приложение
- **Размер сборки**: ~100-500 MB (зависит от платформы)
- **Первый запуск** может занять несколько секунд
- **Все зависимости включены** в сборку

## 🔧 Устранение проблем

### "Unity не найден"
- Установите Unity Hub и Unity Editor
- Убедитесь, что Unity установлен в стандартном месте

### "Сборка не запускается"
- Проверьте логи в `Build/build.log`
- Убедитесь, что все сцены созданы
- Попробуйте собрать вручную через Unity Editor

### "Игра не запускается после сборки"
- Проверьте, что выбрана правильная платформа
- Убедитесь, что все сцены добавлены в Build Settings
- Проверьте консоль на ошибки

## 💡 Советы

- **Тестируйте сборку** перед распространением
- **Создавайте сборки для разных платформ** если нужно
- **Используйте GitHub Releases** для автоматического распространения
- **Добавьте версионирование** в название файла (DroneGame_v1.0.app)

---

**После первой сборки игра будет работать как обычное приложение! 🚁**

