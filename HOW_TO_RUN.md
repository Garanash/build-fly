# 🎮 Как запустить игру (для конечного пользователя)

## 🎯 Цель

Собрать игру **один раз**, чтобы она работала как обычное приложение **БЕЗ Unity Editor**.

## ⚡ Быстрый способ (3 шага)

### Шаг 1: Установите Unity (один раз)

1. Скачайте **Unity Hub**: https://unity.com/download
2. Установите **Unity Editor 2020.3 LTS** или новее
3. Дождитесь завершения установки (~5-10 минут)

### Шаг 2: Запустите скрипт сборки

Откройте терминал в папке проекта и выполните:

```bash
chmod +x build_and_run.sh
./build_and_run.sh
```

Скрипт автоматически:
- ✅ Найдет Unity на вашем компьютере
- ✅ Создаст необходимые сцены
- ✅ Соберет игру в готовое приложение
- ✅ Запустит игру

**Время сборки**: 2-5 минут

### Шаг 3: Готово!

Игра находится в: `Build/DroneGame.app`

**Теперь Unity больше НЕ нужен!** Запускайте игру:

```bash
open Build/DroneGame.app
```

Или просто двойной клик по `DroneGame.app` в Finder.

## 📦 Что происходит

1. **Первый запуск скрипта**: Unity собирает проект в standalone приложение
2. **После сборки**: Игра работает как обычная программа, Unity не нужен
3. **Можно копировать**: `DroneGame.app` можно копировать на другие компьютеры

## 🔄 Если нужно обновить игру

Если вы обновили код из GitHub:

```bash
git pull
./build_and_run.sh
```

Новая версия будет собрана автоматически.

## ❓ Частые вопросы

### Нужен ли Unity для запуска игры?

**Нет!** Unity нужен только для **первой сборки**. После сборки игра работает самостоятельно.

### Можно ли запустить без Unity?

**Да, если уже есть собранная версия** в папке `Build/DroneGame.app`. Просто запустите её.

### Как распространить игру?

Скопируйте папку `Build/DroneGame.app` и отправьте другим. Им Unity не нужен!

### Что делать, если Unity не найден?

1. Убедитесь, что Unity установлен через Unity Hub
2. Проверьте, что Unity находится в `/Applications/Unity Hub/Editor/`
3. Попробуйте собрать вручную через Unity Editor (см. ниже)

## 🛠️ Альтернативный способ (через Unity Editor)

Если скрипт не работает:

1. Откройте проект в Unity Editor
2. `File > Build Settings`
3. Добавьте сцены (создайте их если нужно)
4. Нажмите `Build`
5. Выберите папку `Build`

## 📋 Требования

- **macOS**: 10.14 или новее
- **Unity**: 2020.3 LTS или новее (только для сборки)
- **Место на диске**: ~500 MB для Unity + ~200 MB для сборки

## ✅ Проверка

После сборки проверьте:

```bash
ls -la Build/DroneGame.app
```

Если файл существует - сборка успешна!

---

**Итог**: Один раз собрали → игра работает везде! 🚁

